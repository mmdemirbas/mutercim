- Output dirs should be named after language code (ar, tr, etc.) not full names (arabic, turkish, etc.)
- Rename companions -> people
- Rename cached -> midstate
- rename output folders in alphabetical=semantical order (input, knowledge, l-m-n for cache/intermediate data folder, output, reports)
- output/latex -> should under language folders like output/ar/latex

- Separate development docs (SPEC, phases etc) from actual usage documentation.
- do we still need top-level defaults folder?

- Allow specifying pages per input file

- print live plan and progress
- prettify status output (colors, alignment, tabularization, visualization etc.)
- add more tests
- Consider using a format different from JSON to use less-tokens.
- Add pdf-to-image also as a step -> paginate
- Allow translating to multiple languages in parallel
- What about multi-language input?
- Provide AR/TR/ZH translations for docs
- Support multiple app languages (help messages, logs etc)? (ar/tr/zh)

- Add README
