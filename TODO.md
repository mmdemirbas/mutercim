- Add README
- Add 'task dist'

- (not sure): Output dirs should be named after language code (ar, tr, etc.) not full names I think.
- Add setup commands for windows and unix to install both the binary and completions.
